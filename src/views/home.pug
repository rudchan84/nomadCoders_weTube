extends base
include mixins/video

block potato 
  h2 Welcome here you will see the trending videos~
  //-each potato, idx in videos
    //-li video number #{idx} : #{potato.title}
  //-else 
    //-li Sorry nothing found
  //-iteration
  each potato, idx in videos 
    //-mixin 사용하기
    +video(potato, idx) 
    //-array가 비어있으면 아래로
  else 
    div Sorry nothing found